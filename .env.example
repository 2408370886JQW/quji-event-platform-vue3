# mock：使用本地模拟数据；api：调用后端接口
VITE_DATA_SOURCE=mock

# 后端部署后设置为真实 API 地址，例如：https://api.example.com/api
VITE_API_BASE_URL=/api

# 本地联调时可启用 Vite 反向代理，例如：http://localhost:8080
# VITE_API_PROXY_TARGET=http://localhost:8080

# 本地开发端口
VITE_PORT=5173
