# Vue3 浏览器验收记录

## 2026-09-21 登录页（1280×1100）

- 页面成功加载，视口无横向溢出（`scrollWidth === clientWidth === 1280`）。
- 新疆背景图正常渲染，品牌、语言切换、四个角色卡、账号密码与首次入驻入口完整可见。
- 角色标题保持完整语义，不逐字断行；表单标签与按钮点击区域清晰。
- `/images/` 资源请求未发现失败项。
- 当前登录卡尺寸约 627×799，页面在视口内完整呈现。

后续继续验证登录、侧栏与关键业务路由。

## Manus 登录页基线对照

React 基线与 Vue3 登录页在 1280×1100 视口下结构、图片构图、左右分栏、卡片间距、角色卡与表单区域近乎重合。基线正文使用 PingFang SC / Microsoft YaHei 字体栈，页面底色为 `rgb(245, 246, 248)`；主按钮为 `rgb(37, 94, 200)`、48px 高、8px 圆角，标题为 24px/600，角色标题为 16px/600。Vue3 当前设计令牌与这些值一致，不需要为登录页追加视觉修复。

## 登录交互

主办方预置账号提交成功，应用已生成主办方会话并加载工作台内容；工作台包含近期活动、我的待办、今日售票、待处理事项和异常提醒。首次点击后的截图处于路由切换瞬间，下一步等待稳定页面并检查后台外壳尺寸。

## 工作台外壳（1280×1100）

工作台稳定加载，侧栏实测为 232px，顶栏实测为 64px，顶栏从 x=232 起与侧栏右边界准确对齐。页面无横向溢出，所有图片和 CSS 资源均无失败记录。近期活动、待办、关键数据与异常提醒在首屏内形成明确的主次层级，侧栏图标和顶部按钮均为统一 Lucide 线性图标。

## 工作台基线差异（需修复）

与 Manus React 预览直接对照后，Vue3 后台外壳的尺寸和设计令牌一致，但业务入口和工作台内容仍有差异。React 基线侧栏包含“创建活动”，顶栏包含活动面包屑与三语切换；工作台标题区包含“全部活动 / 创建活动”，近期活动卡含日期、地点与箭头图标；右侧将“我的待办”和“异常提醒”拆为两个区块；底部还有“快捷入口 / 现场核验”及三张业务动作卡。Vue3 当前缺少这些入口或采用了不同布局。后续修复应以 React `Home.tsx` 为结构基线，同时保留 Vue3 路由与状态机。

## 工作台重构后验收

重构后的 Vue3 工作台已恢复 React 基线的双按钮标题区、近期活动图标行、独立待办与异常区、四张业务指标卡以及三张快捷操作卡。1280px 视口下卡片分栏、图标、文案和首屏信息层级与 React 对照图基本重合，日期口径也已恢复为跨日范围。当前截图仍使用清空前的旧入驻状态，因此侧栏“创建活动”尚未出现；登录状态修复已写入代码，需重新登录后复验。

## 会话重置

已清空 Vue3 localStorage 并稳定返回 `/login`，登录页背景、角色卡、表单与语言入口仍正常，说明新增已认证账号初始化逻辑没有破坏首次入驻入口。

## 普通主办方登录与侧栏

清空状态后使用预置主办方账号登录，系统已按 React 基线初始化为已认证主体，侧栏出现“创建活动”，同时保留独立“首次入驻注册”空流程。侧栏收起成功，宽度降为 64px，全部 Lucide 图标居中，顶部面包屑、当前活动档案、三语按钮和通知按钮不再错位；“展开侧边栏”控制仍可见，可恢复完整侧栏。

## 后台外壳交互

侧栏从 64px 状态可正常恢复为 232px。顶栏已出现与 React 一致的“活动管理 > 当前活动”上下文、当前活动档案按钮、三语按钮和通知铃。通知中心已恢复标题、副说明、“全部标记为已读”、三条带 Lucide 图标的提醒、时间和“进入处理”动作，抽屉位置与遮罩符合预览基线。

## 系统设置

系统设置抽屉已与预览结构对齐：可切换四种工作身份，展示当前账号与所属单位，提供审核待办、现场异常、多语言三项开关，并保留权限边界说明和退出登录。抽屉宽度、卡片间距、图标和开关状态均正常。

## 角色服装道具页面

侧栏入口可达，页面数据已按预览恢复为四项业务指标和四条申报记录，包含角色来源、参考图、服装信息、道具信息、申报状态、风险语义、操作记录和查看详情；导出清单与批量处理均已呈现为明确可操作入口。路由切换截图抓取发生在页面更新瞬间，下一步等待稳定画面并检查三张本地图片与详情抽屉。

## 角色申报详情

页面稳定后四张指标卡、八列表格、四条申报记录与 React 基线一致。打开首条详情后，560px 右侧抽屉完整显示参与人、作品来源、状态、风险、角色参考图、服装全身图、道具参考图、服装与道具说明、操作记录以及“退回补充 / 确认通过”动作；三张图片均来自 Vue3 `public/images` 且加载成功。

## 数字档案

数字档案页已恢复三张归档指标卡和五项带 Lucide 图标的归档清单，每项仅“查看”按钮可点击，状态标签和列表间距与 React 预览一致。“生成归档摘要”作为主操作保留，并进入三步任务流程后才提供文件下载，避免表面按钮或无去向提示。

## 归档任务流程

“生成归档摘要”已验证为可连续推进的三步抽屉。第一步展示当前状态、关联内容和操作记录；第二步提供导出范围说明、可编辑备注、默认勾选的操作记录写入选项以及“生成文件”按钮。入口不是死按钮，流程与 React 预览一致。

## 归档文件完成态

归档任务可从范围选择推进到“活动文件已生成”，完成态提供“下载归档摘要”和“返回列表”。下载按钮已实际触发浏览器下载流程，下一步由文件系统确认下载产物。

## 数据中心

数据中心已按 React 基线恢复完整长页面：六行业务口径表、活动数据四指标、售票与入场趋势、数据口径说明、三项归档指标和五项归档清单均存在。顶部和活动数据区各有清晰导出入口，归档区保留生成摘要与查看操作，页面不再用四张装饰卡替代真实业务内容。

## 数据中心稳定画面

数据中心首屏在 1280px 视口下无横向溢出，六行业务表和四张指标卡排版清晰，趋势与口径区位于下方滚动区域。首次点击顶栏“当前活动档案”未发生路由变化，需要改用活动标题入口复查该按钮的点击目标。

## 单场活动数字档案

通过顶栏活动标题成功进入 `/activities/evt-2026-0628`。顶部活动卡、返回按钮、状态、编号、标题、副标题、时间、地点、主办方、导出摘要、现场管理入口、七步生命周期和三张阶段说明均完整。九个带 Lucide 图标的业务页签全部呈现；活动概况将本地海报、基本信息、待办、四项当前数据和操作记录放在与 React 一致的双栏结构中。此前点击“当前活动档案”未跳转是浏览器索引瞬时问题，实际活动标题入口和路由均正常。

## 活动详情票务模块

票务页签可正常切换，包含四项指标、票种配置、订单管理、退款处理、导出数据和票种管理动作，表格支持横向内容。浏览器验收发现 Mock 仍使用旧版 ¥79/¥49 票价与 4,662 张总量不一致；已立即修复为 React 基线的普通观众票 ¥88/3548、Coser 专属票 ¥68/326、学生早鸟票 ¥58/788（停售）、现场当日票 ¥98/0（未开售），总销量保持 4,662。修复触发 HMR 后旧元素索引失效，需刷新后复验订单切换。

## 票务口径复验

HMR 后活动概况已显示“4 类票种已配置，2 类正在售卖”，已售票保持 4,662。票务页签显示库存 8,300，并逐项恢复普通观众票 ¥88 / 3548、Coser 专属票 ¥68 / 326、学生早鸟票 ¥58 / 788（停售）、现场当日票 ¥98 / 0（未开售），与 React 真源一致。

## 票务订单与退款

“订单管理”可显示订单号、购票人、票种、金额、实名信息、订单状态和电子票状态；“退款处理”可显示退款原因、处理状态和查看入口。两者均能在同一活动详情内连续切换，票价和票种沿用统一后的 React 口径。

## 入驻与认证

已认证主办方页面显示五步流程、通过状态、创建活动出口、实名信息、四项主体材料和必填完成 3/3。营业执照预览可打开真实本地图片，明确标注“政府公开样例”和“趣集公开样例 · 非真实证照”水印，并显示上传要求、文件名称、类型和保存时间；部署不依赖 Manus 临时资源。

## 移动端登录页（375×812）

使用本地 Chromium 以 375×812 实际渲染。品牌与三语入口保持顶部单行；介绍区和登录区按纵向卡片排列；标题采用完整语义断行；统计卡为三列且不出现逐字换行；角色卡与账号表单可继续向下滚动。背景图、白色卡片、蓝色主色和字体大小均保持清晰，没有横向溢出。

## 移动端已登录工作台（375×812）

已登录工作台在 375px 下使用 64px 顶栏和单列内容，标题、按钮、活动卡、日期、地点与待办均按完整语义换行，无横向溢出。汉堡按钮可打开与 React 一致的顶部两列菜单，十个业务入口、Lucide 图标、选中态和角色道具角标完整可见，菜单不会遮断顶部返回路径。

## 2026-09-23 身份材料拆分验收

主办方入驻页已将原先合并的“法定代表人身份证明或经办授权材料”拆分为法定代表人身份证正面、身份证反面和经办授权书三个独立文件记录。被授权经办人路径显示 5 项必填，上传顺序为正面、反面、授权书；正面保存前反面不可上传，反面保存前授权书不可上传。实际触发三个文件输入后，完成数从 0/5 依次更新为 1/5、2/5、3/5，文件名分别独立保存。

切换为法定代表人本人办理后，经办授权书上传位自动隐藏，必填总数从 5 项降为 4 项，页面明确提示无需上传经办授权书。补齐营业执照和安全责任人后，被授权经办人路径达到 5/5 并可提交平台审核；切换平台运营角色后，审核页可以看到身份证正面、身份证反面和经办授权书三条独立记录，办理身份显示为“被授权经办人办理”。

430×932 移动端实测无横向溢出（页面宽度、滚动宽度和内容宽度均为 430px）。办理身份选项上下排列，身份材料按单列显示，标题、说明和按钮均按完整语义换行，上传顺序和禁用状态清晰可辨。

本地图片上传已改为保存可持久化的 `data:` 预览。实测上传身份证正面后刷新页面，文件名仍保留，预览图片自然尺寸有效且可正常显示；旧版无法恢复的 `blob:` 地址会在状态迁移时清除，并明确回退到带水印的公开样例，避免审核端出现破图。

## 2026-09-24 首次入驻身份前置修正

首次入驻已重排为“手机号验证 → 身份选择 → 本人实名与主体信息”三步。完成手机号验证后，页面实测没有任何单选项默认选中，也不会提前显示姓名和身份证输入框；首次使用人必须主动选择“我是法定代表人”或“我是被授权经办人”。选择法定代表人后，第三步字段显示为法定代表人姓名、本人身份证号和主办方主体名称，承诺文案不再出现已获授权的错误表述。

保存实名与主体信息后，Vue3 路由直接进入 `/onboarding?focus=materials` 并自动定位主体材料区。法定代表人路径显示 4 项必填且不显示授权书；被授权经办人路径显示 5 项必填，并按身份证正面、反面、经办授权书顺序提供独立上传入口。办理身份在材料页只读展示，不允许在后置步骤随意切换。Manus React 在线预览已同步为同一三步流程和条件材料清单。
